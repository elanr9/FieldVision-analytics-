Uppercase 12px eyebrow that labels every dashboard section.

```jsx
<SectionHeading>Funnel · Last 30 days</SectionHeading>
<SectionHeading count={4}>Activity</SectionHeading>
```

Sentence-case the text (CSS uppercases it). Join qualifiers with " · ".
