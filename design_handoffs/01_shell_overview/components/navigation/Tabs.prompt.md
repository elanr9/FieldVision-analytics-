Equal-width pill tabs in the sticky header; the active pill is ink.

```jsx
<Tabs value={tab} onChange={setTab} tabs={[{key:'overview',label:'Overview'},{key:'onboarding',label:'Onboarding'},{key:'pipeline',label:'Follow ups',badge:17},{key:'users',label:'Users'},{key:'calendar',label:'Calendar'}]} />
```
