Key/value row; stack inside a 12px-radius bordered box with 12–16px side padding. Hide the first row's top border with a wrapper.

```jsx
<div style={{border:'1px solid var(--border-default)',borderRadius:12,padding:'0 16px'}}>
  <MetaRow label="Team" value="Solar SC" /><MetaRow label="Grad year" value="2027" />
</div>
```
