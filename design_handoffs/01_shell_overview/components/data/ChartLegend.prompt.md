Legend row for Recharts line/bar charts.

```jsx
<ChartLegend items={[{label:'Signups',color:'var(--chart-signups)'},{label:'Trial starts',color:'var(--chart-trials)'},{label:'Paid conversions',color:'var(--chart-conversions)'}]} />
```
