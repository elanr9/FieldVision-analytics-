A person row; tap opens the profile, buttons on the right text/call/email.

```jsx
<UserRow user={{name:'Maya Okafor',team:'Solar SC',email:'maya@…',phone:'+1555…',status:'trialing',joined:'Mar 4'}} onOpen={…} />
```

Use `note` + `showStatus={false}` for pipeline rows ("Trial ends in 14h").
